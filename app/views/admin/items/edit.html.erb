<main>
  <div class = "container px-5 px-sm-0">
     <h4 class = "New Registration">
        商品編集
      </h4>
      <%= form_with model: @item, url: admin_item_path, local:true do |f| %>
      <div class = "row">
        <div class = "col-md-8 offset-md-3">
          <table>
            <tr>
              <td style = "padding: 0 0 15px 30px">商品画像</td>
              <td style = "padding: 0 0 15px 30px">
                <%= f.attachment_field :image, placeholder: "商品画像" %>
              </td>
            </tr>
            <tr>
              <td style = "padding: 0 0 15px 30px">商品名</td>
              <td style = "padding: 0 0 15px 30px">
                <%= f.text_field :name, size: "40x5", autofocus: true, id:"inputName" %>
              </td>
            </tr>
            <tr>
              <td style = "padding: 0 0 15px 30px">商品説明</td>
              <td style = "padding: 0 0 15px 30px">
                <%= f.text_area :description, size: "40x5", autofocus: true, id:"inputDescription" %>
              </td>
            </tr>
            <tr>
              <td style = "padding: 0 0 15px 30px">ジャンル</td>
              <td style = "padding: 0 0 15px 30px">
                <%= f.collection_select :genre_id, @genre, :id, :name,{ prompt: "--選択してください--" }  %>
                <!--f.collection_select :prefecture, @prefecture, :id, :name, { prompt: "選択してください" } -->
                 <!--f.collection_select カラム名, 要素の配列, value属性の項目, テキストの項目, { prompt: "選択してください" }-->
              </td>
            </tr>
            <tr>
              <td style = "padding: 0 0 15px 30px">税抜価格</td>
              <td style = "padding: 0 0 15px 30px">
                <%= f.text_field :price, size: "40x5", autofocus: true, id:"inputPrice" %>円
              </td>
            </tr>
            <tr>
              <td style = "padding: 0 0 15px 30px">販売ステータス</td>
              <td style = "padding: 0 0 15px 30px">
                <%= f.radio_button :is_active, :true %>
                <%= f.label :is_active, "販売中", {value: :true, style: "display: inline-block;"} %>
                <%= f.radio_button :is_active, :false %>
                <%= f.label :is_active, "販売停止中", {value: :false, style: "display: inline-block;"} %>
              </td>
            </tr>
            <td style = "text-align: center;">
              <%= f.submit '変更を保存', class: "btn btn-outline-secondary" %>
            </td>
          </table>
        </div>
      <% end %>
    </div>
  </div>
</main>