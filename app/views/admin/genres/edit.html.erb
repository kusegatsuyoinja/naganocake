<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h3>ジャンル編集</h3>
    </div>
  </div>
  
  <div class="row">
    <%= form_with model:@genre, local:true do |f| %>
    <div class="col">ジャンル名</div>
    <div class="col"><%= f.text_field :name %></div>
    <div class="col"><%= f.submit '新規登録', class: "btn btn-success" %></div>
    <% end %>
  </div>